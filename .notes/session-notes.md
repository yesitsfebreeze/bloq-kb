# Session Notes

## 2026-03-08
- Swapped right-thumb `Enter` and `Backspace` on base layer in `config/bloq.keymap`.
- Kept shifted Backspace behavior as Delete by preserving `&bspc` (`behavior-mod-morph`: Backspace -> Delete with Shift).
- Investigated `just build` Docker error and confirmed Compose resolves image name to `cygnus-zmk-build` from project name `cygnus` + service `zmk-build`.
